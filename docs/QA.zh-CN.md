# 0.9 工作台与审稿闭环验证

运行 `pnpm run verify` 验证材料纠正、范围排除、研究状态、任务状态、产物差集和记忆迁移。

隔离浏览器验证入口：

```bash
node scripts/serve-ui-fixture.mjs
```

打开终端打印的本地地址。此页面仅模拟 DSH 状态，不访问论文目录、模型或真实 DSH 设置。

验收路径：

1. 空目录打开工作台，确认首页显示三个场景，不显示评分或设置卡片；高级工具移到“更多”。
   逐一选择“已有论文 / 从零开始 / 论文返修”，检查九个步骤的准备材料、预期结果和任务草稿。
   点“准备任务到对话框”不能自动启动；切换步骤不能确认研究状态。
   “从零开始 → 收集与整理”分别测试本地、联网和混合来源。
2. 在“更多 → 更多工具 → 课题起步”直接启动研究，点击“模拟完成并产出文件”，确认新增 `literature/research-gap.md`
   出现在最近任务，任务待检查而不是自动确认。
3. 在“更多 → 最近任务”验收任务，展开项目记忆填写方向，在流程确认选题；填入文献模块任务，确认携带记忆。
4. 切换场景重新准备时替换旧插件任务，保留前后手动文字；已编辑或未知草稿需确认替换，取消时原文不变。
5. 切换第二项目，将 `sources/seed.pdf` 归为参考文献库，重新扫描，确认选择保留。
   代码审查 YAML 默认被排除。返回第一项目，确认记忆、场景、步骤和阶段仍在。
6. 模拟失败，确认最近任务显示失败。控制台可通过 `fixture.setFailSave(true)`
   模拟保存失败，检查错误提示和原记录保留，然后恢复为 `false`。
7. 在更多工具导出项目 JSON，重新导入预览后应用，确认记忆、场景和归类恢复且任务历史不被导出覆盖。

真实 DSH 页面另外检查侧栏开合、四视图、配置保存和草稿填入。隔离会话验证不代表
在线文献检索质量或 PDF 解析能力已经经过真实研究任务评估。

## 0.8.0 验证记录

- `pnpm run verify`：18 项测试通过，包括九个引导任务的中英文生成、场景配置经过宿主 schema 和快照往返，以及原有材料/任务回归。
- Playwright 隔离浏览器：空白起步、主题输入、联网来源、返修回复证据要求、已有论文入口、跨项目场景/步骤恢复、草稿追加和禁止自动发送通过。
- 保存失败、只读设置、侧栏收起、高级评分入口、模块直接执行及模拟产物回流通过；修复重复组件 key 后，隔离页控制台无错误/警告。
- 本机 Web profile 安装 0.8.0 并重启 DSH，确认注册 20 个技能/命令；真实宿主页面确认三个新手入口可见、高级工具默认折叠和侧栏开合正常。
- 未调用模型、未提交真实论文任务；PDF 解析、在线检索及生成质量不在本次验收范围。

## 0.8.1 界面减负验证

- 首页仅三个文字入口；选定后只有一个主操作。项目记忆、投稿指南、最近任务与高级工具都在“更多”，不占首页。
- 补充主题默认折叠，收集资料时直接显示；步骤选择改为单个下拉框，随时可切换。
- 准备任务后出现发送指引，切换步骤后清除旧成功提示；不自动发送、不覆盖已有草稿。
- 21 项自动测试通过，新增首屏结构、单主按钮、资料收集表单测试。
- 浏览器验证覆盖三场景、跨项目步骤恢复、草稿追加、高级功能入口，以及 390 × 740 窄屏下无横向溢出、主操作在首屏可见；另检查模拟深色主题。
- 真实 DSH 中修复原生详情栏自动收起后的侧栏遮挡：在 1440、1036、768 像素窗口宽度下检查对话框与侧栏不重叠，关闭插件侧栏后额外预留空间恢复为零。更窄屏幕仍使用可关闭浮层。

## 0.8.2 草稿替换回归

- 24 项测试通过，新增插件任务替换、前后手动文字保留和编辑/未知/重复块确认保护。
- Playwright 验证从零起步切到已有论文、返修，以及重复准备任务不叠加；三种入口仍不自动发送。
- 编辑原任务、取消替换、确认期间再次编辑、跨项目草稿保护均通过；隔离页控制台无错误。
- 刷新页面或组件重新挂载后，不凭文字内容猜测旧任务归属；未知草稿先确认。旧版本自动追加行为已由此规则取代。

## 0.9.0 审稿闭环验证

- 30 项测试通过，新增 DSH 用户标记与结束轮次关联、长事件窗口已观测关联保留、排除推理/中断消息、结果协议校验、意见编号和新稿版本一致性、实验/决策保护、宿主 schema 持久化、导出排除执行状态。
- 隔离浏览器：点击“载入模拟审稿回复”，在工作台展开闭环并整理；控制台 `fixture.completeReview()` 模拟当前阶段的完成回复（使用宿主声明的真实消息结构，不调用模型）。
- 完整链路：整理 → 授权修改 → 自动复核 → 本轮结束；三份结果关联用户/助手序号，实验项保持 open，结束后不多发请求。
- 验证第二轮到达上限、输入草稿暂停续行并原文保留、手动恢复复核、暂停后续执行。
- `fixture.completeReview({malformed:true})` 模拟错误结果；`fixture.addUnrelatedMessage()` 模拟新用户消息干扰，均阻止自动继续。
- 此验收是协议和控制流测试，不表示真实模型、PDF 读取或学术修改质量已经经过端到端评估。真实研究请先用副本做小范围试验。
- 本机安装并重启 0.9.0；真实 DSH 会话中验证完整助手正文可被列为来源，中断任务不会列入。该检查未发送模型请求、未修改论文文件。
